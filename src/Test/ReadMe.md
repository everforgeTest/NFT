# Test Instructions

- Ensure a HotPocket instance is running locally (ws://localhost:8081) and the contract has been deployed.
- Install test dependencies:

```
cd src/Test
npm install
```

- Run tests:

```
npm test
```
